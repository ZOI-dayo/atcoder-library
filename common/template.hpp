#pragma once

#include <bits/stdc++.h>

using namespace std;

// --- 型エイリアス ---
using ll = long long;
template <typename T> using vec = vector<T>;

// --- 制御マクロ ---
#define rep(i, n) for (ll i = 0; i < n; ++i)
#define all(v) begin(v), end(v)
